services:
  - type: web
    name: taskdo
    runtime: node
    plan: free
    buildCommand: npm run render:build
    startCommand: npm run render:start
    autoDeploy: true
    envVars:
      - key: ENCRYPTION_KEY
        sync: false
